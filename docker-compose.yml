version: "3"

services:
  backend:
    build: ./backEnd
    ports:
      - "3001:3001"
    networks:
      - app-network

  frontend:
    build: ./frontEnd
    ports:
      - "8080:80"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
